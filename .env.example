# ============================================================
# Hivemind Inc — Environment Variables
# ============================================================
# This file is the single source of truth for all env vars.
# Copy to .env.local and fill in real values:  cp .env.example .env.local
#
# Legend:
#   [Secret Manager]  = stored in GCP Secret Manager for production
#   [Cloud Run env]   = passed via --set-env-vars in deploy.sh
#   [local only]      = not needed in production
# ============================================================

# --- Database ---------------------------------------------------
# [Secret Manager] DATABASE_URL
# Local: connects via Cloud SQL Auth Proxy on localhost
# Prod:  injected from Secret Manager secret "DATABASE_URL"
DATABASE_URL=postgresql://postgres:PASSWORD@localhost:5432/hivemind

# [Cloud Run env] INSTANCE_UNIX_SOCKET
# Do NOT set locally. On Cloud Run, this tells db/index.ts to
# connect via Unix socket instead of TCP.
# INSTANCE_UNIX_SOCKET=/cloudsql/hivemind-inc:us-central1:hivemind-db

# --- Email (SMTP via Google Workspace) -------------------------
# [Cloud Run env] SMTP_USER — Google Workspace account
SMTP_USER=akshith@hivemind.inc

# [Secret Manager] SMTP_PASS — Google App Password (16 chars)
SMTP_PASS=xxxx xxxx xxxx xxxx

# [Cloud Run env] SMTP_FROM — Display name + alias for outbound mail
SMTP_FROM="Hivemind" <welcome@hivemind.inc>

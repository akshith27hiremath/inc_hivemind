# === Local Development ===
# Connect via Cloud SQL Auth Proxy on localhost
DATABASE_URL=postgresql://postgres:PASSWORD@localhost:5432/hivemind
# Do NOT set INSTANCE_UNIX_SOCKET locally

# === Production (Cloud Run) — set via gcloud, not here ===
# DATABASE_URL is injected from Secret Manager
# INSTANCE_UNIX_SOCKET=/cloudsql/hivemind-inc:us-central1:hivemind-db

# === Phase 2: Email notifications ===
# RESEND_API_KEY=re_xxxxxxxxxxxxx
# NOTIFY_EMAIL=your@email.com
